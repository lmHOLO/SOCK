export type Ad = {
  id: number;
  image: string;
  link: string;
  title: string;
};

export type SnackListItemType = {
  id: number;
  image: string;
  title: string;
};

export type ThemeType = {
  id: number;
  image: string;
  title: string;
};
